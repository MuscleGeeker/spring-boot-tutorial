spring:
  application:
    name: bootstrap
  cloud:
    config:
      uri: http://localhost:8080
      profile: dev
      label: master
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest